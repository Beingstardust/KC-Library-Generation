# Docs Vs Code Truth Table

| Surface | Claim | Code / repo reality | Judgment |
| --- | --- | --- | --- |
| `README.md:57-80` | Use the clean entry surfaces instead of the research-era `steps/` tree, and the repo is ready for first use. | The new public scripts do not execute the pipeline. `python scripts/local/run_stage.py --json intake` returned `ready_for_first_run: true`, but the retained multi-PDF intake wrapper fails immediately because `data/raw/pdfs` no longer exists. | Misleading |
| `docs/workflows/offline_generation_lifecycle.md:62` | The new operator commands point to the same underlying logic. | `src/kc_l/runtime/operator_stages.py:80-82` exposes intake only as Step 1, Step 2, Step 3, and `operator_stages.py:91-94` exposes draft generation only as Step 5.3, Step 6.6, Step 6.7, Step 6.8. But `configs/pipeline/main_quest.base.yaml:55-67` says the preserved internal flow also includes Step 3.5, Step 3.6, Step 4, Step 4.5, Step 5, and Step 5.2. The public mapping is not the same underlying logic. | Misleading |
| `docs/architecture/operator_repo.md` | The public control surface is `scripts/local/run_stage.py` and `configs/`, with the legacy step tree retained only internally. | Real execution still depends on retained `steps/` resource YAMLs and old path conventions (`data/raw`, `data/processed/.../_sets`, historical manifest dates). The new public control surface does not bridge those dependencies. | Partially honest, but overstates continuity |
| `docs/workflows/hpc_first_run_preparation.md` | The repo is prepared for a future textbook-scale HPC run. | `python scripts/hpc/render_main_quest_config.py --mode hpc_gpu` returns `ok: true`, but `configs/pipeline/main_quest.hpc_gpu.example.yaml:7,17-20` hardcodes a Linux interpreter and `/beegfs2/...` paths, and the validator only checks placeholders and a few layout paths. | Misleading |
| `scripts/local/run_stage.py` + `src/kc_l/runtime/operator_stages.py:228-276` | Status / intake / draft preflight can tell whether the repo is ready. | The readiness logic is layout-driven. It does not validate the retained step resource YAMLs or old-path assumptions. As a result it reports `support_ready: true` and `ready_for_first_run: true` while Step 2 still requires `data/raw/...`. | Misleading |
| `scripts/maintenance/check_operator_repo.py` | Repo check gives an operator-facing truth surface. | It reduces the same `build_operator_repo_status()` result and therefore inherits the same blind spots. In this repo it returned `ok: true` even though the retained Step 2 wrapper cannot run on the clean layout. | Misleading |
| `README.md:80` | The repo is intentionally empty of runtime state. | The current clean worktree already contains three PDFs and one hierarchy file under `data/input/`, and the public status command warns about them. This is a current-state docs mismatch rather than a core parity failure. | Stale / low-severity mismatch |
| `steps/README.md` | Use the new entrypoints instead of the internal legacy steps. | The new entrypoints do not include any actual corpus runner, Step 4/5 execution surface, or late-stage model-backed launcher. The internal steps are still the only real pipeline lane. | Misleading if read as operational equivalence |
